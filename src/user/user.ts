export interface User {
  discordUsername?: string;
  acalaAddress?: string;
  email?: string;
  discordId?: string;
  twitterAddress?: string;
  tweetLink?: string,
  emailMission?: string,
  twitterMission?: string,
  missionXp?: Number,
  existingXp?: Number,
  totalXp?: Number,
  inServer?: Number,
  role?: string
}