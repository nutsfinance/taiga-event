export interface User {
  discordUsername?: string;
  acalaAddress?: string;
  email?: string;
  discordId?: string;
  twitterAddress?: string;
  tweetLink?: string,
  acalaMission?: boolean,
  emailMission?: boolean,
  twitterMission?: boolean,
  missionXp?: Number,
  existingXp?: Number,
  totalXp?: Number,
  inServer?: boolean,
  role?: string
}